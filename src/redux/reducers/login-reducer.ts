type AuthStateType = {}

const initialState: AuthStateType = {}

export const loginReducer = (state: AuthStateType = initialState, action: AuthActionsType) => {
    return state
}

//types
type AuthActionsType = {}